/**
 * Replaced at download time by the Venture OS that packages this extension,
 * which writes its own address here so the operator never types it.
 *
 * This checked-in placeholder exists so the unpacked source also loads: an
 * absent file would make popup.html log a 404 on every open, and a missing
 * default is a blank field rather than a broken popup.
 */
globalThis.VENTURE_DEFAULT_BASE_URL = "";
